# Editor / IDE
.idea
.vscode
*.swp
*~

# OS
.DS_Store
Thumbs.db

# Tooling
.claude/
.mcp.json
node_modules/

# Shell dotfiles that tooling may mount into the project root
.bash_profile
.bashrc
.profile
.zprofile
.zshrc
.gitconfig
.gitmodules
.ripgreprc

# Standalone export of the contract animation, for sharing on its own
contracts-demo.html

# Standalone export of the contract animation, for sharing on its own
contracts-demo.gif

# LaTeX build artifacts from resume/, which builds in place
resume/*.pdf
resume/*.aux
resume/*.log
resume/*.out
resume/*.synctex.gz
resume/*.fls
resume/*.fdb_latexmk
# Superseded resume drafts; git history carries versions now
resume/*.bak-*
